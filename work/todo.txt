1. При редактиране на въпроси да листва само един екземпляр от всеки използван контекст.
2 инсталиране на библиотеки (когато кача на сървъра работата с писмените тестове):
pip install docxtpl
pip install python-docx  # docxtpl го използва като зависимост
pip install Pillow  # за работа с изображения

media_files/
├── templates/      # за съхранение на Word шаблони
├── generated/      # за съхранение на генерирани документи
└── temp/           # за временни файлове при работа с изображения